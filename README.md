# Focused Labs Helm Charts

## Usage
```bash
helm repo add focused https://focused-labs.github.io/charts
helm search repo focused
helm install my-release focused/<chart>
```
## Auto deployed with a GH Workflow
